# Drutinycs Helper 🛠️

This tool automates the `drutinycs wap` workflow for the Support Team. It ensures we don't forget to generate **Autopanic Reports**, without being annoying.

## ✨ Features

* **Smart Prompt:** Asks if you want to run the check when you open your terminal.
* **Conflict Detection:** Checks if `drutinycs` is already running in another tab/window. If it is, it stays silent.
* **P10k Fix:** Automatically configures Zsh to prevent "Instant Prompt" error warnings.

## 🚀 Installation

Run the following commands in your terminal:

```bash
# 1. Clone the repository
git clone [https://github.com/sourabhpb/Drutinycs-helper.git](https://github.com/sourabhpb/Drutinycs-helper.git) ~/Drutinycs-helper

# 2. Run the installer
cd ~/Drutinycs-helper && bash install.sh